
<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>ساعة S20 Pro Max + سماعات — صفحة منتج</title>
  <meta name="description" content="ساعة ذكية S20 Pro Max مع سماعات داخل العلبة. شاشة كبيرة، تتبع نشاط، وإشعارات. اطلب الآن." />

  <style>
    :root{
      --bg:#0b0f17; --muted:#97a3b6; --text:#e9eef8;
      --brand:#ff7a18; --brand2:#4dd2ff; --ok:#31d07a;
      --ring: rgba(255,122,24,.35);
      --shadow: 0 18px 55px rgba(0,0,0,.45);
      --radius: 18px; --radius2: 26px; --max: 1120px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial;
      background: radial-gradient(1200px 600px at 25% 0%, rgba(77,210,255,.16), transparent 55%),
                  radial-gradient(1200px 650px at 80% 0%, rgba(255,122,24,.20), transparent 55%),
                  var(--bg);
      color:var(--text); line-height:1.7;
    }
    a{color:inherit;text-decoration:none}
    img{max-width:100%;display:block}
    .container{max-width:var(--max); margin:0 auto; padding:0 18px;}

    .nav{
      position:sticky; top:0; z-index:20;
      backdrop-filter: blur(12px);
      background: rgba(11,15,23,.55);
      border-bottom:1px solid rgba(255,255,255,.06);
    }
    .nav-inner{display:flex; align-items:center; justify-content:space-between; padding:14px 0; gap:12px;}
    .brand{display:flex; align-items:center; gap:10px; font-weight:800;}
    .badge{
      font-size:12px; padding:6px 10px; border-radius:999px;
      border:1px solid rgba(255,255,255,.10);
      background: rgba(255,255,255,.05);
      color:var(--muted);
    }
    .nav-links{display:flex; gap:14px; align-items:center; flex-wrap:wrap; justify-content:flex-end;}
    .nav-links a{font-size:13px;color:var(--muted)}
    .nav-links a:hover{color:var(--text)}

    .btn{
      display:inline-flex; align-items:center; justify-content:center; gap:10px;
      padding:12px 16px; border-radius:999px;
      border:1px solid rgba(255,255,255,.10);
      background: rgba(255,255,255,.06);
      color:var(--text); font-weight:700; cursor:pointer;
      transition:.2s;
    }
    .btn:hover{transform:translateY(-1px); background: rgba(255,255,255,.08)}
    .btn.primary{
      background: linear-gradient(135deg, var(--brand), #ffb11a);
      border-color: rgba(255,255,255,.08);
      color:#101010;
      box-shadow: 0 0 0 6px var(--ring);
    }

    .hero{padding:42px 0 18px;}
    .hero-grid{display:grid; grid-template-columns: 1.05fr .95fr; gap:22px; align-items:stretch;}
    @media (max-width: 920px){ .hero-grid{grid-template-columns:1fr;} }

    .hero-card{
      background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius2);
      box-shadow: var(--shadow);
      padding:24px;
    }
    .hero h1{margin:0 0 10px; font-size: clamp(28px, 4vw, 44px); line-height:1.25;}
    .sub{color:var(--muted); font-size:16px; margin:0 0 18px; max-width: 60ch;}

    .pill-row{display:flex; flex-wrap:wrap; gap:10px; margin:16px 0 18px;}
    .pill{
      padding:8px 12px; border-radius:999px;
      border:1px solid rgba(255,255,255,.10);
      background: rgba(255,255,255,.05);
      font-size:13px; display:flex; align-items:center; gap:8px;
    }
    .dot{width:10px;height:10px;border-radius:999px;background:var(--brand2); box-shadow:0 0 0 6px rgba(77,210,255,.12);}

    .price-row{display:flex; flex-wrap:wrap; align-items:flex-end; gap:14px; margin:18px 0 10px;}
    .price{font-size:38px; font-weight:900;}
    .strike{color:rgba(233,238,248,.55); text-decoration:line-through; font-weight:600}
    .note{color:var(--muted); font-size:13px; margin:0}
    .cta-row{display:flex; flex-wrap:wrap; gap:12px; margin-top:16px;}

    .gallery-card{
      border-radius: var(--radius2);
      overflow:hidden;
      border:1px solid rgba(255,255,255,.10);
      box-shadow: var(--shadow);
      background: rgba(255,255,255,.03);
    }
    .gallery-top{padding:14px 16px; display:flex; justify-content:space-between; align-items:center; border-bottom:1px solid rgba(255,255,255,.08);}
    .gallery-top strong{font-size:13px}
    .gallery-top span{font-size:12px; color:var(--muted)}
    .grid4{display:grid; grid-template-columns: repeat(2, 1fr); gap:1px; background: rgba(255,255,255,.08);}
    .grid4 img{aspect-ratio: 1/1; object-fit:cover; background:#0a0a0a;}

    .section{padding:26px 0;}
    .section h2{margin:0 0 10px; font-size:24px;}
    .section p.lead{margin:0 0 18px; color:var(--muted); max-width: 80ch}

    .cards{display:grid; grid-template-columns: repeat(3, 1fr); gap:14px;}
    @media (max-width: 920px){ .cards{grid-template-columns:1fr;} }
    .card{
      background: rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      padding:16px;
    }
    .card h3{margin:0 0 6px; font-size:16px}
    .card p{margin:0; color:var(--muted); font-size:14px}
    .icon{
      width:40px;height:40px;border-radius:12px;
      display:grid; place-items:center;
      background: rgba(255,122,24,.14);
      border:1px solid rgba(255,122,24,.25);
      margin-bottom:10px;
    }

    .split{display:grid; grid-template-columns: 1fr 1fr; gap:14px; align-items:start;}
    @media (max-width: 920px){ .split{grid-template-columns:1fr;} }
    .list{margin:0; padding-right: 18px; color: var(--muted); font-size:14px;}
    .list li{margin:8px 0;}

    .seller{
      background: linear-gradient(180deg, rgba(77,210,255,.08), rgba(255,122,24,.06));
      border:1px solid rgba(255,255,255,.12);
      border-radius: var(--radius2);
      padding:18px;
      box-shadow: var(--shadow);
    }
    .seller-grid{display:grid; grid-template-columns: 1.2fr .8fr; gap:14px; align-items:start;}
    @media (max-width: 920px){ .seller-grid{grid-template-columns:1fr;} }

    .field{display:grid; gap:6px; margin-bottom:12px;}
    label{font-size:12px; color: var(--muted)}
    input, textarea, select{
      width:100%; padding:12px 12px; border-radius: 14px;
      border:1px solid rgba(255,255,255,.10);
      background: rgba(11,15,23,.60);
      color: var(--text); outline:none;
    }
    textarea{min-height:96px; resize:vertical}

    .footer{padding:28px 0 50px; color: var(--muted); border-top:1px solid rgba(255,255,255,.08); margin-top:20px;}
  </style>
</head>

<body>
  <div class="nav">
    <div class="container">
      <div class="nav-inner">
        <div class="brand">
          <span style="display:inline-grid;place-items:center;width:34px;height:34px;border-radius:12px;background:rgba(255,122,24,.18);border:1px solid rgba(255,122,24,.28);font-weight:900;">S</span>
          <div>
            <div style="font-weight:900;line-height:1;">S20 Pro Max</div>
            <div style="font-size:12px;color:var(--muted);line-height:1.2;">ساعة ذكية + سماعات</div>
          </div>
          <span class="badge" id="stockBadge">متوفر</span>
        </div>

        <div class="nav-links">
          <a href="#features">المميزات</a>
          <a href="#specs">المواصفات</a>
          <a href="#seller">معلومات البائع</a>
          <a class="btn primary" href="#order">اطلب الآن</a>
        </div>
      </div>
    </div>
  </div>

  <header class="hero">
    <div class="container">
      <div class="hero-grid">
        <div class="hero-card" id="order">
          <div class="pill-row">
            <div class="pill"><span class="dot"></span> باك كامل: ساعة + سماعات</div>
            <div class="pill">تصميم 49mm</div>
            <div class="pill">شاشة 3D </div>
          </div>

          <h1>باك اقتصادي بثمن 200 درهم — كيهديك قيمة كبيرة.</h1>
          <p class="sub">
            ساعة <b>S20 Pro Max</b> مع <b>سماعات</b> وأحزمة إضافية — للاستعمال اليومي، الرياضة، والإشعارات.
          </p>

          <div class="price-row">
            <div>
              <div class="price" id="priceMain">200 درهم</div>
              <div class="note">الدفع عند الاستلام متاح</div>
            </div>
            <div>
              <div class="strike" id="priceOld">299 درهم</div>
              <div class="note"><span style="color:var(--ok);font-weight:900;">عرض محدود</span></div>
            </div>
          </div>

          <p style="margin:12px 0 0;color:rgba(233,238,248,.62);font-size:12px;">
       
          </p>
        </div>

        <div class="gallery-card">
          <div class="gallery-top">
            <strong>صور المنتج</strong>
            <span>بدّل الروابط بصورك</span>
          </div>
          <div class="grid4">
            <img src="12qaidi.jpeg" alt="صورة الساعة" />
            <img src="123qaidi.jpeg" alt="محتويات العلبة 1" />
            <img src="1234qaidi.jpeg" alt="محتويات العلبة 2" />
            <img src="12345qaidi.jpeg" alt="واجهة العلبة" />
          </div>
        </div>
      </div>
    </div>
  </header>

  <section class="section" id="features">
    <div class="container">
      <h2>المميزات</h2>

      <div class="cards">
        <div class="card"><div class="icon">⌚</div><h3>تصميم كبير وكيبان فاخر</h3><p>ستايل 49mm وشكل عصري.</p></div>
        <div class="card"><div class="icon">💬</div><h3>إشعارات فالمعصم</h3><p>مكالمات ورسائل وتنبيهات التطبيقات.</p></div>
        <div class="card"><div class="icon">🎧</div><h3>سماعات داخل الباك</h3><p>باك كامل بثمن اقتصادي.</p></div>
        <div class="card"><div class="icon">🫀</div><h3>تتبع صحي</h3><p>حسب الإعلان: مراقبة النبض والنشاط.</p></div>
        <div class="card"><div class="icon">🏃</div><h3>مناسبة للرياضة</h3><p>أوضاع نشاط متعددة وإحصائيات سريعة.</p></div>
        <div class="card"><div class="icon">🔁</div><h3>أحزمة إضافية</h3><p>بدّل الستايل ديالك (حسب الدفعة).</p></div>
      </div>
    </div>
  </section>

  <section class="section" id="specs">
    <div class="container">
      <div class="split">
        <div class="card" style="padding:18px;">
          <h2 style="margin-top:0;">المواصفات</h2>
          <div class="list" style="color:var(--muted)">
            <li><b>الموديل:</b> S20 Pro Max </li>
            <li><b>الحجم:</b> ستايل 49mm</li>
            <li><b>الشاشة:</b> 3D Screen</li>
            <li><b>المحتوى:</b> ساعة + سماعات + أحزمة</li>
          </div>
        </div>
        <div class="card" style="padding:18px;">
          <h2 style="margin-top:0;">مناسب لـ</h2>
          <ul class="list">
            <li>هدية ممتازة بثمن 200 درهم</li>
            <li>الرياضة والتتبع اليومي</li>
            <li>الطلاب والتنقل والإشعارات</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="section" id="seller">
    <div class="container">
      <div class="seller">
        <div class="seller-grid">
          <div>
            <h2 style="margin:0 0 8px;">معلومات البائع</h2>
            <p class="lead" style="margin-bottom:14px;">عمّر هاد البيانات باش الزبون يثق ويطلب بسرعة.</p>

            <div class="split">
              <div>
                <div class="field"><label>اسم المتجر</label><input id="sellerName" placeholder="K7I" /></div>
                <div class="field"><label>رقم الهاتف</label><input id="sellerPhone" placeholder="+212675958697" /></div>
                <div class="field"><label>رابط واتساب</label><input id="sellerWhats" placeholder="https://wa.me/0675958697" /></div>
              </div>
              <div>
                <div class="field"><label>المدينة</label><input id="sellerCity" placeholder="الدار البيضاء" /></div>
                <div class="field"><label>التوصيل</label>
                  <select id="delivery">
                    <option>نفس اليوم "الدار البيضاء</option>
                    <option>1–2 أيام لمدن أخرى</option>
                    <option>استلام من المحل</option>
                  </select>
                </div>
                <div class="field"><label>طريقة الدفع</label>
                  <select id="payment">
                    <option>الدفع عند الاستلام</option>
                    <option>تحويل / بطاقة</option>
                    <option>الدفع عند الاستلام + تحويل</option>
                  </select>
                </div>
              </div>
            </div>

            <div class="field">
              <label>الضمان / الاستبدال</label>
              <textarea id="policy" placeholder="مثال: 7 أيام استبدال في حالة عيب صنع."></textarea>
            </div>

            <div class="cta-row">
              <button class="btn primary" onclick="generateSellerCard()">إرسال</button>
            </div>
          </div>

          <div class="card" style="padding:18px;">
            <h3 style="margin-top:0;">إرسال معلومات المشتري الى الواتساب</h3>
            <div id="sellerCard" style="white-space:pre-wrap; font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace; font-size:12.5px; color: rgba(233,238,248,.92); background: rgba(0,0,0,.22); border:1px solid rgba(255,255,255,.08); border-radius:14px; padding:12px;">
ستظهر هنا…
            </div>

            <div style="height:12px"></div>
            <div class="cta-row">
              <a class="btn primary" id="btnWhats" href="#" target="_blank" rel="noopener">اطلب عبر واتساب</a>
              <a class="btn" id="btnCall" href="#">اتصل</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <div style="display:flex;justify-content:space-between;gap:14px;flex-wrap:wrap;">
        <div>
          <div style="font-weight:900;color:var(--text)">صفحة منتج S20 Pro Max</div>
          <div style="font-size:13px;">الثمن: 200 درهم — بدّل الصور والمعلومات حسب متجرِك.</div>
        </div>
        <div style="font-size:13px;"><span style="color:var(--muted)">©</span> <span id="year"></span></div>
      </div>
    </div>
  </footer>

  <script>
    // إعدادات سريعة
    const CONFIG = {
      priceNow: 200,
      priceOld: 299,
      currency: "درهم ",
      stockText: "متوفر"
    };

    document.getElementById("year").textContent = new Date().getFullYear();
    document.getElementById("priceMain").textContent = CONFIG.priceNow + " " + CONFIG.currency.trim();
    document.getElementById("priceOld").textContent = CONFIG.priceOld + " " + CONFIG.currency.trim();
    document.getElementById("stockBadge").textContent = CONFIG.stockText;

    function val(id){ return document.getElementById(id).value.trim(); }

    function generateSellerCard(){
      const name = val("sellerName") || "اسم البائع";
      const phone = val("sellerPhone") || "رقم الهاتف";
      const wa = val("sellerWhats") || "رابط واتساب";
      const city = val("sellerCity") || "المدينة";
      const delivery = document.getElementById("delivery").value;
      const payment = document.getElementById("payment").value;
      const policy = val("policy") || "سياسة الضمان/الاستبدال غير محددة.";

      const text =
`✅ معلومات البائع
🏪 المتجر: ${name}
📍 المدينة: ${city}
📞 الهاتف: ${phone}
💬 واتساب: ${wa}

🚚 التوصيل: ${delivery}
💳 الدفع: ${payment}

🛡️ الضمان/الاستبدال:
${policy}

📦 المنتج: ساعة S20 Pro Max + سماعات (باك كامل)
💰 الثمن: ${CONFIG.priceNow} درهم`;

      document.getElementById("sellerCard").textContent = text;

      const btnCall = document.getElementById("btnCall");
      const btnWhats = document.getElementById("btnWhats");

      const phoneDigits = phone.replace(/[^\d+]/g, "");
      btnCall.href = phoneDigits ? `tel:${phoneDigits}` : "#";
      btnWhats.href = wa.startsWith("http") ? wa : "#";
    }

    async function copySellerCard(){
      const txt = document.getElementById("sellerCard").textContent;
      try{
        await navigator.clipboard.writeText(txt);
        alert("تم النسخ ✅");
      }catch(e){
        alert("تعذر النسخ. قم بتحديد النص ونسخه يدويًا.");
      }
    }
  </script>
</body>
</html>
